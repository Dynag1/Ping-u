# ============================================
# Ping ü - Requirements
# Compatible Windows & Raspberry Pi / Linux
# ============================================

# --- Core Dependencies (tous systèmes) ---
Flask>=2.3.0
Flask-SocketIO>=5.3.0
Flask-Cors>=4.0.0
python-socketio>=5.8.0
python-engineio>=4.5.0
requests>=2.31.0
cryptography>=41.0.0
urllib3>=2.0.0

# --- Excel / XML ---
openpyxl>=3.1.0
xmltodict>=0.13.0

# --- Windows Only (GUI) ---
PySide6>=6.5.0; sys_platform == 'win32'
qt-themes; sys_platform == 'win32'

# --- Linux / Raspberry Pi (async server) ---
eventlet>=0.33.0; sys_platform != 'win32'
gevent>=23.0.0; sys_platform != 'win32'

# --- SNMP (optionnel - température équipements) ---
# Désinstaller pysnmp classique et utiliser le fork maintenu :
# pip uninstall -y pysnmp && pip install pysnmp-lextudio
pysnmp-lextudio>=6.0.0; python_version >= '3.10'
pyasn1>=0.4.8
pyasn1-modules>=0.2.8
pycryptodomex>=3.18.0
